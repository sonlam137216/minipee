# FE-001 Acceptance Criteria Evidence

| Acceptance criterion | Evidence | Command | Result | Relevant file |
| --- | --- | --- | --- | --- |
| Malformed auth JSON does not crash the app. | `malformed stored seller JSON is cleared and redirects to login` asserts rendering does not throw. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| Auth JSON without a token is treated as invalid. | `stored auth without a token is cleared and redirects to login` covers seller-only storage. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| Auth JSON without valid seller data is treated as invalid. | `stored auth without seller data is cleared and redirects to login` covers token-only storage. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| Invalid seller shape is treated as invalid. | `stored auth with invalid seller shape is cleared and redirects to login` covers missing required seller fields. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| Invalid auth storage is cleared. | Invalid-storage tests assert both `marketplace.accessToken` and `marketplace.seller` are removed. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| User is treated as unauthenticated after invalid auth storage is detected. | Invalid-storage tests assert the seller login page renders. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/app/ProtectedRoute.tsx` |
| Protected routes still redirect unauthenticated users to login. | Existing `seller pages require authentication` test remains passing. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/app/ProtectedRoute.tsx` |
| Valid stored auth state still works. | `valid stored auth renders protected seller page and loads products` asserts protected page renders and sends the stored token to the product list request. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| Login continues to work. | `login form submission stores token and calls login API` asserts token and seller are stored in the existing format. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| Logout continues to work. | `logout clears stored auth and returns to unauthenticated navigation` asserts both auth keys are removed. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| Recovery does not send malformed, partial, or invalid stored tokens to protected API requests during startup. | Invalid-storage tests assert `fetch` is not called during recovery. | `cd frontend && npm test -- App.test.tsx` | Pass | `frontend/src/app/App.test.tsx`, `frontend/src/features/auth/AuthContext.tsx` |
| No backend authentication or authorization behavior changes. | Manual diff review verifies no backend files changed. | `git status --short`; diff review | Pass | `backend/` unchanged |
| No new marketplace feature, API endpoint, dependency, or storage mechanism is added. | Manual diff review verifies only frontend auth/test and evidence changes, with no dependency manifest changes. | `git status --short`; diff review | Pass | `frontend/package.json` unchanged |
