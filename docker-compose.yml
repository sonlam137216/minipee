services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: marketplace
      POSTGRES_PASSWORD: marketplace
      POSTGRES_DB: marketplace_dev
    ports:
      - "5432:5432"
    volumes:
      - marketplace_postgres:/var/lib/postgresql/data

volumes:
  marketplace_postgres:
